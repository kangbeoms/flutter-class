import 'package:flutter/material.dart';

class WidgetDart {
  Widget portraitMode() {
    return const Stack(
      children: [
        Center(
          child: Text('Portrait Mode'),
        )
      ],
      
    );
  }
    Widget landscapeMode() {
    return const Stack(
      children: [
        Center(
          child: Text('Portrait Mode'),
        )
      ],
      
    );
  }
}